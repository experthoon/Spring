package spring.upload.dragon;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.servlet.ModelAndView;


/*
 * @Controller public class EachReadController {
 * 
 * @GetMapping("/") public String form1() { return "updateform2"; }
 * 
 * @GetMapping("/board/read1") public ModelAndView read1(@RequestParam String
 * name, @RequestParam int age) { ModelAndView mview = new ModelAndView();
 * 
 * mview.addObject("name", name); mview.addObject("age", age);
 * 
 * mview.setViewName("uploadresult2");
 * 
 * return mview;
 * 
 * }
 * 
 * }
 */